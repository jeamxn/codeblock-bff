{
  "name": "@codeblock-bff/server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "mongodb": "^6.12.0",
    "@notionhq/client": "^2.2.15",
    "dayjs": "^1.11.13",
    "@codeblock-bff/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
